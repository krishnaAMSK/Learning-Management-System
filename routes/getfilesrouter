const express = require('express');
const router = express.Router();

const getfilescontroller = require('../controllers/getfilescontroller');
router.get('/',getfilescontroller.getFilesByUserId);

module.exports = router;
